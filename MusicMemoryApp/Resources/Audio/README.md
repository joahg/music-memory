# Audio Clips

Bundle 30-second excerpts in this folder using the exact filenames referenced by `local/MusicMemoryCore/Sources/MusicMemoryCore/Resources/pieces.json`.

If you have full recordings, use `scripts/prepare_audio_clips.sh` with `data/audio-clips-template.csv` to create the app-ready clips automatically.
